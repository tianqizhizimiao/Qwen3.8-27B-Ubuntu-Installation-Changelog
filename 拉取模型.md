## 从魔搭上拉取模型
### 安装依赖
```bash
python -m pip3 install modelscope -i https://pypi.tuna.tsinghua.edu.cn/simple
```
- 建议在虚拟环境中进行操作

### 下载模型
```python
from modelscope.hub.file_download import model_file_download

model_id = "Abiray/Qwen3.8-27B-Q4_K_M-GGUF"
file_name = "Qwen3.8-27B-Q4_K_M.gguf"

file_path = model_file_download(
    model_id=model_id,
    file_path=file_name,
    local_dir="./", # 文件保存到此目录
    revision="master"
)

print("文件下载完成，路径：", file_path)
```
- 请根据实际环境与需求拉取对应模型
